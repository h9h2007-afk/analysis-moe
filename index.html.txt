<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة تحليل البيانات | وزارة التعليم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Cairo', sans-serif; background-color: #F9F9F9; scroll-behavior: smooth; }
        .bg-moe-green { background-color: #1E5B3C; }
        .bg-moe-light { background-color: #72B132; }
        .text-moe-gold { color: #C5A059; }
        /* تأثير الحركة عند التحميل */
        .fade-in { animation: fadeIn 1s ease-in; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        /* تأثير التفاعل مع البطاقات */
        .stat-card:hover { transform: scale(1.03); transition: all 0.3s ease; cursor: pointer; }
    </style>
</head>
<body class="fade-in">

    <div class="flex flex-col md:flex-row min-h-screen">
        <aside class="w-full md:w-72 bg-moe-green text-white p-6 shadow-xl z-20">
            <div class="mb-10 text-center">
                <div class="text-2xl font-bold">وزارة <span class="text-moe-light">التعليم</span></div>
                <div class="text-xs text-moe-gold mt-1">المملكة العربية السعودية</div>
            </div>
            <nav class="space-y-3" id="main-nav">
                <button onclick="updateData('general')" class="nav-link w-full flex items-center gap-3 py-3 px-4 rounded-lg bg-moe-light text-white font-semibold shadow-md">📊 الرئيسية</button>
                <button onclick="updateData('chemistry')" class="nav-link w-full flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-white/10 transition">🧪 كيمياء (زيد بن ثابت)</button>
                <button onclick="alert('سيتم تفعيل التقارير قريباً')" class="nav-link w-full flex items-center gap-3 py-3 px-4 rounded-lg hover:bg-white/10 transition">📜 تقارير الإرشاد</button>
            </nav>
        </aside>

        <main class="flex-1 p-6 md:p-10">
            <header class="flex justify-between items-center mb-8 border-b pb-5">
                <div>
                    <h2 id="page-title" class="text-2xl font-bold text-gray-800">نظام تحليل البيانات الإحصائي</h2>
                    <p class="text-gray-500 text-sm">مرحباً أ/ حسن، أنت تشاهد البيانات الآن</p>
                </div>
                <button onclick="window.print()" class="bg-moe-gold text-white px-4 py-2 rounded-lg hover:bg-yellow-700 shadow transition">طباعة التقرير PDF</button>
            </header>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
                <div class="stat-card bg-white p-6 rounded-2xl shadow-sm border-t-4 border-moe-green">
                    <p class="text-gray-400 text-xs font-bold uppercase mb-1">إجمالي الطلاب</p>
                    <h3 id="stat-1" class="text-3xl font-bold text-gray-800">6,120,450</h3>
                </div>
                <div class="stat-card bg-white p-6 rounded-2xl shadow-sm border-t-4 border-moe-light">
                    <p class="text-gray-400 text-xs font-bold uppercase mb-1">المعلمون النشطون</p>
                    <h3 id="stat-2" class="text-3xl font-bold text-gray-800">520,100</h3>
                </div>
                <div class="stat-card bg-white p-6 rounded-2xl shadow-sm border-t-4 border-moe-gold">
                    <p class="text-gray-400 text-xs font-bold uppercase mb-1">نسبة الإنجاز</p>
                    <h3 id="stat-3" class="text-3xl font-bold text-gray-800">98.5%</h3>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                <div class="bg-white p-6 rounded-2xl shadow-md">
                    <h4 class="mb-6 font-bold text-gray-700 border-r-4 border-moe-green pr-2">تحليل الأداء</h4>
                    <canvas id="dynamicLineChart"></canvas>
                </div>
                <div class="bg-white p-6 rounded-2xl shadow-md">
                    <h4 class="mb-6 font-bold text-gray-700 border-r-4 border-moe-gold pr-2">توزيع المهارات</h4>
                    <canvas id="dynamicPieChart"></canvas>
                </div>
            </div>
        </main>
    </div>

    <script>
        let lineChart, pieChart;

        function initCharts(data1, data2) {
            const ctxLine = document.getElementById('dynamicLineChart').getContext('2d');
            const ctxPie = document.getElementById('dynamicPieChart').getContext('2d');

            lineChart = new Chart(ctxLine, {
                type: 'line',
                data: {
                    labels: ['الأسبوع 1', 'الأسبوع 2', 'الأسبوع 3', 'الأسبوع 4'],
                    datasets: [{
                        label: 'المستوى العام',
                        data: data1,
                        borderColor: '#1E5B3C',
                        backgroundColor: 'rgba(30, 91, 60, 0.1)',
                        fill: true,
                        tension: 0.4
                    }]
                },
                options: { responsive: true, plugins: { tooltip: { enabled: true, mode: 'index', intersect: false } } }
            });

            pieChart = new Chart(ctxPie, {
                type: 'doughnut',
                data: {
                    labels: ['عملي', 'نظري', 'أبحاث'],
                    datasets: [{
                        data: data2,
                        backgroundColor: ['#C5A059', '#1E5B3C', '#72B132']
                    }]
                }
            });
        }

        // دالة لتغيير البيانات تفاعلياً
        function updateData(type) {
            if (type === 'chemistry') {
                document.getElementById('page-title').innerText = "نتائج اختبارات الكيمياء - زيد بن ثابت";
                document.getElementById('stat-1').innerText = "145 طالب";
                document.getElementById('stat-2').innerText = "92%";
                document.getElementById('stat-3').innerText = "85.0";
                lineChart.data.datasets[0].data = [70, 85, 80, 95];
            } else {
                document.getElementById('page-title').innerText = "نظام تحليل البيانات الإحصائي";
                document.getElementById('stat-1').innerText = "6,120,450";
                document.getElementById('stat-2').innerText = "520,100";
                document.getElementById('stat-3').innerText = "98.5%";
                lineChart.data.datasets[0].data = [85, 87, 86, 90];
            }
            lineChart.update();
        }

        initCharts([85, 87, 86, 90], [30, 45, 25]);
    </script>
</body>
</html>